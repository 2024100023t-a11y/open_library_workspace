name: open_library_api
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.8.1'

dependencies:
  http: ^1.2.0
  logging: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0